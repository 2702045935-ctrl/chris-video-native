# CHRIS视频 · 原生版（云端打包）

照着 `桌面/s/` 里那张抖音首页截图做的原生 iOS App，纯 SwiftUI，不是网页壳。

## 一、出包（2~4 分钟）

1. 在 `work/iosfull` 里 `git add -A && git commit -m "改了什么" && git push`
2. 打开 https://github.com/2702045935-ctrl/chris-video-native/actions
3. 等最新那条 **build-ios** 变绿 ✅
4. 页面底部 **Artifacts** → 下载 **CHRISVideo-unsigned-ipa**
5. 解压得到 `CHRISVideo-unsigned.ipa`

## 二、装到 iPhone

1. 爱思助手 → 应用游戏 → 导入 → 选 `.ipa`
2. 用自己的 Apple ID 签名（免费账号就行）
3. 手机上：设置 → 通用 → VPN与设备管理 → 信任证书

## 三、文件说明

| 文件 | 作用 |
| --- | --- |
| `Sources/Core.swift` | 全部尺寸/颜色 token（来自参考图像素测量） |
| `Sources/Icons.swift` | 手绘图标（三横/搜索/爱心/评论/星/分享/音符/加号） |
| `Sources/FeedView.swift` | 首页：全屏视频 + 上下滑切换 |
| `Sources/TopBar.swift` | 顶部 tab 条 + 搜索 |
| `Sources/RailView.swift` | 右侧头像/赞/评论/收藏/分享/拍同款 |
| `Sources/CaptionView.swift` | 左下 @作者、文案、音乐 |
| `Sources/BottomBar.swift` | 底栏 首页/朋友/＋/消息(65)/我 |
| `Sources/Pages.swift` | 搜索、朋友、消息、我 |
| `Assets/` | 视频封面、头像（打包进 App） |
